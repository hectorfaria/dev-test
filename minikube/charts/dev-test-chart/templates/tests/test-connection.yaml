apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "dev-test-chart.fullname" . }}-test-connection"
  labels:
    {{- include "dev-test-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "dev-test-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
